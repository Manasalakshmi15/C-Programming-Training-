Wriye a program to add two numbers using pointers.

Program:
#include<stdio.h>
int add(int *a,int *b);
int main ()
{

    int num_1 = 24,num_2 = 34;
    int *ptr1=&num_1;
    int *ptr2=&num_2;
    int sum=add(ptr1,ptr2);
    printf("sum is %d",sum);
    return 0;
}
int add(int *a,int *b)
{
    int result = *a + *b;
    return result;
}

Output:
sum is 58
