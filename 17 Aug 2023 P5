Write a program to find the square root of a number using standard library functions.

#include <stdio.h>
#include <math.h>

int main() {
    double number, squareRoot;

    printf("Enter a number: ");
    scanf("%lf", &number);

    squareRoot = sqrt(number);

    printf("Square root of %.2lf is %.2lf\n", number, squareRoot);

    return 0;
}
