Write a program to calculate the volume of the speher using funtions
#include<stdio.h>
#define PI 3.14
float vol_of_sphere (int r);

float vol_of_sphere (int r)
{
    float result = (4/3)*PI*(r*r*r);
    return result;
}

int main()
{
    int r;
    printf("Enter the radius of the circle:");
    scanf("%d",&r);
    float result = vol_of_sphere(r);
    printf("The volume of spehere is %.2f",result);
    return 0;
}
