Write a program to return the absolute value of a long integer. Test with positive and negative values.

#include <stdio.h>
#include <stdlib.h>

long absolute(long num) {
    if (num < 0) {
        return -num; 
    } else {
        return num; 
    }
}

int main() {
    long a = 10;
    long b = -20;

    printf("Absolute value of %ld is %ld\n", a, absolute(a)); 
    printf("Absolute value of %ld is %ld\n", b, absolute(b));

    return 0;
}
