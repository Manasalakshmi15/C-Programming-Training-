Write a program to read a string and print it in reverse order.

Program:
#include<stdio.h>
int main ()
{
    char s[10],s1[10];
    int length;
    printf("The string is: ");
    scanf("%[^\n]s",&s);
    length=strlen(s);
    for(int i=0;i<length;i++)
    {
      s1[i]= s[length-1-i];
    }
    printf("\nThe reversed string is %s",s1);
    return 0;
}

Output:
The string is: manasa

The reversed string is asanam
