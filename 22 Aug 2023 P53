Write a program that accepts 3 integers and find the biggest number of the 3 (without using if condition or library functions)
#include <stdio.h>

int main() 
{
    int a,b,c, max;

    printf("Enter the 3 integer: ");
    scanf("%d%d%d", &a,&b,&c);

    max = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);

    printf("The biggest number among the three integers is: %d\n", max);

    return 0;
}
