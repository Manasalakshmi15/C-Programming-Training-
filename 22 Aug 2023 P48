Calculate the area of the circle that fits perfectly within a squre having length of side a.
#include <stdio.h>
#include <math.h>

int main() 
{
    float a, radius, area;

    printf("Enter the side length of the square: ");
    scanf("%f", &a);

    radius = a / 2;
    area = 3.14159 * radius * radius;

    printf("The area of the circle that fits within the square is: %.2f\n", area);

    return 0;
}
