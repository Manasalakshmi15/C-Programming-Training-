Write a program to print the following characters in a reverse way: 'X', 'M', 'L'

#include <stdio.h>

int main() {
    char characters[] = {'X', 'M', 'L'};
    int length = sizeof(characters) / sizeof(char);

    for (int i = length - 1; i >= 0; i--) {
        printf("%c ", characters[i]);
    }

    return 0;
}
