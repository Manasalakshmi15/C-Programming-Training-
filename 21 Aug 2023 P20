Use enum datatype to store days of week.
#include<stdio.h>
int main()
{
  enum weekday {SUN, MON, TUE, WED, THU, FRI, SAT};
  enum weekday day;
  printf("%d %d %d %d %d %d %d",SUN, MON, TUE, WED, THU, FRI, SAT);
  return 0;
}
